import {
  normalizeNutritionData as normalizeShared,
  parseServingSizeGrams as parseShared,
} from '@health-scanner/shared';

export const normalizeNutritionData = normalizeShared;
export const parseServingSizeGrams = parseShared;
